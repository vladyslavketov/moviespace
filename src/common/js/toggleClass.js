export function toggleClass(ref, className) {
  ref.classList.toggle(className);
}

export function addClass(ref, className) {
  ref.classList.add(className);
}

export function removeClass(ref, className) {
  ref.classList.remove(className);
}